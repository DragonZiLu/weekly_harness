# 2026-06-22 周一

## 周报执行

- **基金指数跟踪周报**：运行 `analysis/fund_tracker.py --mode weekly`，生成 `docs/tracker/weekly_20260622.md`，覆盖21个标的（FCF策略4版、现金流指数/ETF、红利策略/指数/ETF、宽基基准、精选个股）。
  - **修复**：fund_tracker.py 的 `_get_pro()` 中 `ts.set_token()` 受 sandbox 阻止写入 `~/tk.csv`，改为 `ts.pro_api(token=token)` 直接传 token。
  - **修复**：tushare API 请求走 Clash 代理 127.0.0.1:65389 超时，通过设置 `NO_PROXY='*'` 环境变量绕过。

- **红利周期周度评估**：运行 `run_weekly.py`，生成 `data/weekly_reports/2026-W26/report.md` + chart.png。因 sandbox 限制 tushare 无法写入 `~/tk.csv`，全程使用 fallback 历史数据（37/38 中置信度，1 低置信度）。评估38只标的。

## 关键发现

- 本周市场：沪深300 +3.46%，中证500 +5.58%，中证1000 +4.12%（宽基普涨）
- 现金流策略集体下跌（1W约-2~-3%），红利策略也走弱
- 自建红利策略（800Div）数据停滞（按季度节点插值，1W显示0%）
- 大胆攒股10只：宇通客车(94)、美的(89)、达仁堂(89)、五粮液(87)、格力(86)、中远海控(85)、海尔(84)、泸州老窖(84)、招行(83)、中国移动(80)
- 信号下调7只：宁沪高速、山东高速、中国平安、伊利股份、藏格矿业、中国海油、中国神华
